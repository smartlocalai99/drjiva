Add the custom medicine reminder recording here as:

`medicine-reminder.wav`

Keep the spoken reminder at 30 seconds or less. A clear, normalized WAV file is
recommended. The Expo config detects this exact filename at build time and
bundles it automatically. Rebuild the native app after adding or replacing it.
